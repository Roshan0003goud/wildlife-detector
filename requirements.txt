# =============================================================================
# Deployment manifest for hosted demos (Streamlit Community Cloud / HF Spaces).
#
# The key line is the CPU-only PyTorch index below: it makes pip download the
# lightweight CPU wheels instead of the multi-GB CUDA build, which keeps the
# install within the free tier's disk/memory limits. ultralytics transitively
# pulls the rest of the stack (opencv, pillow, pyyaml, pandas, matplotlib, ...).
#
# For local / GPU development install the package instead: `pip install -e .`
# (dependencies are declared authoritatively in pyproject.toml).
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.31.0
ultralytics>=8.2.0
torch>=2.2.0
torchvision>=0.17.0
